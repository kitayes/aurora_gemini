# Быстрый старт: Большие объемы контекста

## Проблема решена! ✅

Теперь вы можете добавлять **тысячи символов** lore информации, и бот будет находить нужное.

## Как это работает

### 📝 Создайте большой текстовый файл

```
lore/extended/history_full.md
```

```markdown
# История Империи Аврора

## Основание (2000 лет назад)

[10,000 символов детальной истории...]

## Золотой век

[Еще 5,000 символов...]

## Война магов

[И еще 8,000 символов...]
```

**Итого:** 23,000 символов информации!

### 🚀 Проиндексируйте файл

```bash
# Автоматический выбор стратегии (рекомендуется)
go run cmd/index_large/main.go \
  --file lore/extended/history_full.md \
  --zone history \
  --tags "история,империя,война"

# Или всю папку разом
go run cmd/index_large/main.go \
  --dir lore/extended \
  --zone extended_lore \
  --tags "детальный,полный"
```

### 🎯 Что произойдет

Файл будет разбит на оптимальные chunks:

```
history_full.md (23,000 символов)
├── Summary (300 символов) - краткий обзор
├── Раздел "Основание" (2,000 символов)
├── Раздел "Золотой век" (1,500 символов)
├── Раздел "Война магов" (2,500 символов)
└── ... остальные разделы
```

**Результат:** 10-15 chunks вместо 1 огромного!

### 💬 Пользователь спрашивает

> "Расскажи о войне магов"

**Старый подход:** Находит общий chunk с историей → неточный ответ

**Новый подход (RAG + chunking):**
1. Находит summary → понимает контекст
2. Находит раздел "Война магов" → точная информация
3. Бот дает подробный, релевантный ответ ✅

## Примеры файлов

### Пример 1: История (15,000 символов)

```markdown
# Подробная история мира Аврора

## Древние времена
[3000 символов]

## Эпоха магии
[4000 символов]

## Современность
[3000 символов]
```

### Пример 2: Система магии (10,000 символов)

```markdown
# Магическая система Авроры

## Основы
[2000 символов]

## Школы магии
### Огонь
[1500 символов]

### Вода
[1500 символов]

### Воздух
[1500 символов]
```

### Пример 3: Энциклопедия рас (20,000 символов)

```
Энциклопедия рас и народов

Люди
  Происхождение: [1000 символов]
  Культура: [1500 символов]
  
Эльфы
  Происхождение: [1000 символов]
  Культура: [2000 символов]
  
И так далее...
```

## Команды

### Индексация одного файла

```bash
go run cmd/index_large/main.go --file путь/к/файлу.md --zone world --tags история
```

### Индексация папки

```bash
go run cmd/index_large/main.go --dir lore/extended --zone extended
```

### С выбором стратегии

```bash
# Hierarchical (для очень больших файлов)
go run cmd/index_large/main.go --file history.md --strategy hierarchical

# Paragraph (для средних файлов)
go run cmd/index_large/main.go --file magic.md --strategy paragraph
```

## Рекомендации

### ✅ DO (Делайте)

- Создавайте структурированные .md файлы с заголовками
- Используйте `## Заголовки` для разделов
- Храните большие тексты в `lore/extended/`
- Индексируйте через `index_large` команду

### ❌ DON'T (Не делайте)

- Не добавляйте 20k символов в один JSON chunk
- Не используйте неструктурированный текст (без абзацев)
- Не забывайте про теги для фильтрации

## Производительность

| Размер файла | Chunks | Время индексации | Время поиска |
|--------------|--------|------------------|--------------|
| 2,000 символов | 1-2 | ~0.5 сек | ~250ms |
| 5,000 символов | 3-5 | ~1 сек | ~250ms |
| 10,000 символов | 6-10 | ~2 сек | ~250ms |
| 20,000 символов | 12-18 | ~3.5 сек | ~250ms |

**Важно:** Время поиска не увеличивается! RAG найдет нужный chunk из тысяч.

## Теперь вы можете

✅ Хранить энциклопедии lore (десятки тысяч символов)  
✅ Получать точные, релевантные ответы  
✅ Разбивать большие документы автоматически  
✅ Использовать summary для быстрого обзора  
✅ Находить конкретные детали среди тысяч chunks

**Больше никаких ограничений на размер lore! 🎉**

# 🌌 Aurora Gemini RPG Engine
[![Go Version](https://img.shields.io/badge/go-1.22+-blue.svg)](https://go.dev/)
[![AI](https://img.shields.io/badge/AI-Gemini_1.5_Flash-orange.svg)](https://ai.google.dev/)
[![Database](https://img.shields.io/badge/DB-SQLite_%26_pgvector-green.svg)](https://www.postgresql.org/)

**Aurora Gemini** — это высокотехнологичный движок для текстовых ролевых игр (TRPG) в сеттинге мрачного фэнтези. Бот воплощает образ «Сферы Лапидария» — древнего артефакта, который выступает в роли ИИ-ведущего, управляющего миром на основе контекстных знаний (RAG).

---

## 🛠 Ключевые Технологии

### 💎 1. Google Gemini API (LLM Layer)
Это «мозг» проекта. Нейросеть используется не просто для чата, а как полноценный игровой движок:
* **Интент-анализ**: Понимание намерений игрока (атака, исследование, диалог) и их валидация.
* **Нормализация данных**: Автоматический парсинг свободных текстовых анкет в структурированный JSON для БД.
* **Guardrails**: Система ограничений, предотвращающая выход ИИ за рамки сеттинга и правил мира.

### 🧠 2. RAG & Vector Search (pgvector)
Технология **Retrieval-Augmented Generation** решает проблему «галлюцинаций» ИИ и дает ему доступ к огромному лору:
* **Семантический поиск**: Вместо поиска по ключевым словам, система использует эмбеддинги для поиска фрагментов лора по смыслу.
* **pgvector**: Использование расширения PostgreSQL для хранения векторов и мгновенного нахождения нужных знаний (религии, география) среди тысяч записей.
* **Чанкинг**: Автоматическое разбиение текстов на смысловые блоки для подачи максимально точного контекста в промпт.

### 🏗 3. Clean Architecture (Golang)
Проект написан на Go с акцентом на модульность и производительность:
* **Service Layer**: Логика квестов, боевки и перемещений полностью отделена от внешних API.
* **Repository Pattern**: Абстракция доступа к данным (SQLite для персонажей и PostgreSQL для векторов).
* **Concurrency**: Использование горутин для параллельной обработки сотен игровых сессий без задержек.

### 💾 4. Persistence & State Management
* **Изолированные миры**: Каждый игрок находится в своей «сцене», состояние которой (история, инвентарь, статы) сохраняется в БД.
* **Система миграций**: Автоматическое обновление структуры таблиц при добавлении новых игровых механик.

---

## 🛠 Технический Стек

* **Backend**: Go (Golang) — высокая производительность и надежная работа с горутинами.
* **LLM**: Google Gemini API (модели Flash/Pro) для генерации сюжета и анализа действий.
* **Storage**: 
    * **SQLite**: Локальное хранение статов персонажей, инвентаря и истории сцен.
    * **PostgreSQL + pgvector**: Поиск по эмбеддингам для работы со сложным лором.
* **Interface**: VK SDK — работа через сообщения сообщества.

---

## 🏗 Архитектура Проекта

Проект следует принципам **Clean Architecture**:
-   `internal/delivery/vk`: Слой обработки входящих событий и команд.
-   `internal/service`: Основная бизнес-логика (персонажи, квесты, логика GM).
-   `internal/repository`: Работа с данными (индексация векторов, SQL-запросы).
-   `internal/llm`: Оркестрация системных промптов, парсинг ответов нейросети и управление токенами.
-   `internal/rag`: Логика чанкинга и извлечения релевантных знаний.

---

## 🚦 Инструкция по запуску

### 1. Переменные окружения
Создайте файл `.env` в корневой директории:
```env
VK_TOKEN=ваш_токен_вк
GEMINI_KEY=ваш_api_ключ_google
GM_USER_ID=ваш_vk_id_для_прав_админа
DB_PATH=./data/aurora.db

Развертывание:
```
docker-compose up --build
```

Наполнение базы знаний:
```
go run cmd/indexer/main.go
```

Доступные команды:
```
!анкета
!ход [текст]
!лапидарий
!сюжет
!финансы
!команды
!бой
```